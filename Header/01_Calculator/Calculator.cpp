#include "Calculator.h"

int Calculator::Add(int a, int b)
{  
    return a + b;
    // Adder adder(a, b);
    // return adder.Process();
}

int Calculator::Subtract(int a, int b)
{
    return a - b;
}